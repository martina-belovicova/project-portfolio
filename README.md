# Martina Belovičová – Project Portfolio

React/Vite portfolio prepared for GitHub Pages.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The GitHub Pages workflow in `.github/workflows/deploy.yml` deploys the `dist` folder automatically after pushes to `main`.
